python-telegram-bot==21.6
anthropic==0.40.0
python-dotenv==1.0.1
mcp[cli]==1.3.0
ruff==0.8.0

# scripts deps (upload_capsule_photos.py)
Pillow>=10.0.0
google-cloud-storage>=2.0.0

# test deps
pytest
pytest-asyncio
telethon<1.43  # 1.43.x ships speculative layer 225 message ctor (9cb490e9) that breaks deserialization of production messages (9815cec8)
